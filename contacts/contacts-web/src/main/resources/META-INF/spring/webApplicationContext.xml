<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.1.xsd">

<!-- NOTE: spring.data.repository is currently a system property. This is intended to work with maven, e.g.,
	mvn tomcat:run -Dspring.data.repository=jpa (or gemfire). This property also activates the maven profile 
	to enable the associated dependencies. When running on tcServer from STS, rather than setting a system 
	property it is more convenient to simply edit the default value here. Also, 
-->
	<import resource="classpath:/META-INF/spring/contacts-${spring.data.repository:jpa}/applicationContext*.xml"/>
	
</beans>
